extends ./base

block content
    div#section
        h1 movies
        p #{movieItems.length}개의 영화정보

        ul
            each movie in movieItems
                img(src=movie.image)
                h2 #{movie.title}
                p #{movie.content}
